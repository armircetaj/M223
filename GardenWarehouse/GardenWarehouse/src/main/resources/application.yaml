spring:
  application:
    name: GardenWarehouse

server:
  port: 8080

---

spring:
  config:
    activate:
      on-profile: dev

  datasource:
    generate-unique-name: false
    #url: jdbc:h2:file:./h2-file/warehouse-dev
    url: jdbc:h2:mem:warehouse-dev
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update

  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
      data-locations: classpath:db/data-dev.sql

---

spring:
  config:
    activate:
      on-profile: prod

  datasource:
    generate-unique-name: false
    url: jdbc:h2:file:./h2-file/warehouse-prod
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update

  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
      data-locations: classpath:db/data-prod.sql